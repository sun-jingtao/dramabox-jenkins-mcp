{
  "name": "dramabox-jenkins-mcp",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "dramabox-jenkins-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "npm run clean && tsc",
    "self-check": "npm run build && node dist/index.js --self-check",
    "prepack": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.5.0"
  }
}
